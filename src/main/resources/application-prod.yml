spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://apart-db.internal:5432/postgres
    username: postgres
    password: cBeOtR1s2Br0yAN
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: false
        format_sql: false
        use_sql_comments: false